# MySQL configuration
spring.datasource.url=jdbc:mysql://localhost/webcms?characterEncoding=UTF-8&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=123456aA

# Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=create

# Compress all static content
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,application/javascript,text/html,text/xml,text/plain

# Unique security secret to sign access tokens
webcms.security.secret=secrettoken
